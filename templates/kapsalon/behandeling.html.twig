{% extends 'base.html.twig' %}

{% block behandeling %}
    <div class="container-fluid">
        <div class="row">
            {% for behandeling in behandelings %}
                <div class="card" style="width: 18rem;">
{#                    <img src="{{ asset(behandeling.img) }}" class="card-img-top" alt="...">#}
                    <div class="card-body">
                        <h5 class="card-title">{{ behandeling.description }}</h5>
                        <p class="card-text">{{ behandeling.price }}</p>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}